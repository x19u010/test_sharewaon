<%= form_with(model: dialy) do |form| %>
  <% if dialy.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(dialy.errors.count, "error") %> prohibited this dialy from being saved:</h2>

      <ul>
        <% dialy.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
